specVersion: "v1beta"
serviceId: "tem"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tem-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
